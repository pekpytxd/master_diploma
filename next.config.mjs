/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com', 'your-other-domain.com', 'encrypted-tbn0.gstatic.com', 'i.imgur.com']
    },
};

export default nextConfig;
